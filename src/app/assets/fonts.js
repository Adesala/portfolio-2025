import { Wallpoet, Oswald, Inter, Space_Grotesk } from 'next/font/google';

export const wallpoet = Wallpoet({ weight: '400', subsets: ['latin'], display: 'swap' });
export const oswald = Oswald({ weight: ['400', '700'], subsets: ['latin'], display: 'swap' });
export const inter = Inter({ weight: ['400', '700'], subsets: ['latin'], display: 'swap' });
export const spaceGrotesk = Space_Grotesk({ weight: ['400', '700'], subsets: ['latin'], display: 'swap' });